<div class="container">
  <div class="row">
     <h3 class="category-name"><%= @category.name %></h3>
  </div>
  <div class="row">
    <p><%= link_to 'Edit', edit_category_path(@category) %> | 
    <%= link_to 'Delete', category_path(@category) %></p>
  </div>
</div>